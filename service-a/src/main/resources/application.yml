server:
  port: 9001

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-lb:8761/eureka
  instance:
    prefer-ip-address: true

spring:
  application:
    name: api-gateway